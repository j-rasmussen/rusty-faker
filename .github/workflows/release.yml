name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # One wheel per Python version per target. Linux builds every interpreter the
  # manylinux container provides in a single job; macOS and Windows use a matrix.
  linux:
    name: wheel linux-${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.10"
      - uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --find-interpreter
          manylinux: auto
          sccache: "true"
      - uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.target }}
          path: dist

  macos:
    name: wheel macos-${{ matrix.target }}
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        target: [aarch64, x86_64]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python
          sccache: "true"
      - uses: actions/upload-artifact@v7
        with:
          name: wheels-macos-${{ matrix.target }}-${{ matrix.python-version }}
          path: dist

  windows:
    name: wheel windows-x64 py${{ matrix.python-version }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          architecture: x64
      - uses: PyO3/maturin-action@v1
        with:
          target: x64
          args: --release --out dist -i python
          sccache: "true"
      - uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-x64-${{ matrix.python-version }}
          path: dist

  sdist:
    name: sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - uses: actions/upload-artifact@v7
        with:
          name: wheels-sdist
          path: dist

  verify:
    name: verify wheel py${{ matrix.python-version }} ${{ matrix.os }}
    needs: [linux, macos, windows, sdist]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist
      - name: Metadata renders
        run: |
          python -m pip install --upgrade pip twine
          twine check dist/*
      # --only-binary means a missing wheel for this interpreter fails here rather
      # than silently falling back to building the sdist.
      - name: Install and smoke test
        shell: bash
        run: |
          python -m pip install --only-binary :all: --no-index --find-links dist rusty-faker
          python -c "import rusty_faker; print(rusty_faker.__version__); print(rusty_faker.Faker().name())"
          # The compat conftest imports the real Faker, so the whole suite needs it;
          # addopts = "-p no:faker" keeps its pytest plugin out of the way.
          python -m pip install "pytest>=8" "Faker==40.39.0"
          python -m pytest tests -q

  verify-sdist:
    name: verify sdist builds from source
    needs: [sdist]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.10"
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/download-artifact@v8
        with:
          name: wheels-sdist
          path: dist
      - name: sdist must carry build.rs and the data tables
        run: |
          tar tzf dist/*.tar.gz | grep -q 'rusty-faker-core/build.rs'
          tar tzf dist/*.tar.gz | grep -q 'rusty-faker-core/data/en_US/person.json'
      - name: Build and install from sdist
        run: |
          python -m pip install --no-binary :all: dist/*.tar.gz
          python -c "import rusty_faker; print(rusty_faker.Faker().address())"

  publish:
    name: publish to PyPI
    # Disabled until you turn it on. To enable:
    #   1. On PyPI, add a pending Trusted Publisher: project rusty-faker, owner
    #      j-rasmussen, repository rusty-faker, workflow release.yml, environment pypi.
    #   2. Create the GitHub environment "pypi" (a required reviewer is worth it).
    #   3. Set the repository variable PYPI_PUBLISH_ENABLED to true.
    #   4. Push a v* tag. An unset variable evaluates false, so nothing publishes
    #      before then, and workflow_dispatch can never publish.
    if: ${{ vars.PYPI_PUBLISH_ENABLED == 'true' && startsWith(github.ref, 'refs/tags/v') }}
    needs: [verify, verify-sdist]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/rusty-faker
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist
      - uses: pypa/gh-action-pypi-publish@release/v1
